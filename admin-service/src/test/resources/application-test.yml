spring:
  application:
    name: admin-service
  main:
    allow-bean-definition-overriding: true
  data:
    redis:
      host: localhost
      port: 6379

eureka:
  client:
    enabled: false

server:
  port: 8089
